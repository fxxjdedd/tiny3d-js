import { Scene } from "../scene";
import { Render } from "./render";

export class RenderManager {
    renderScene(render: Render, scene: Scene) {}
    renderTransparent(render: Render, scene: Scene) {}
}
